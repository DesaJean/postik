Manual screenshots & demo GIF go here.

To do before tagging a public release:
  1. Run `npm run tauri dev` and arrange 4-5 notes with different colors and a running timer.
  2. Record demo.gif (LICEcap on macOS, ScreenToGif on Windows).
       Recommended: 800x500, 12fps, 4-5s loop.
  3. Take a hero screenshot named `hero.png` for the GitHub social card.
  4. Replace this placeholder file once you have the assets.

Optional but nice:
  - controller.png  — main controller window
  - timer-running.png — note with active countdown
  - colors.png — set of notes in all 6 colors
